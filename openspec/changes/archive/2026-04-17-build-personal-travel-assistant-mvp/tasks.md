## 1. MVP 技术占位决策

- [x] 1.1 明确 MVP 阶段的信息源使用静态或示例数据，并写清时效性边界与后续替换策略
- [x] 1.2 明确 `preferenceProfile` 首版采用静态对象或 `localStorage` 占位，不引入后端持久化
- [x] 1.3 明确 `/planner` 作为独立页面存在，而不是与首页合并

## 2. 数据模型准备

- [x] 2.1 定义 `planDraft` 数据结构，覆盖目的地输入、候选路线、交通对比、酒店区域建议与讨论日志
- [x] 2.2 定义 `preferenceProfile` 数据结构，覆盖稳定偏好、推断偏好、证据来源与信心度
- [x] 2.3 为现有 `trip` 结构补充来源字段和决策摘要，建立 `planDraft -> trip` 的映射规则

## 3. 规划流程原型

- [x] 3.1 新增规划入口与输入页，支持录入目的地、人数、预算、日期和偏好约束
- [x] 3.2 使用静态或示例数据渲染 A/B/C 候选路线卡片，而不是直接生成最终分享页
- [x] 3.3 在候选方案中展示交通方式对比、酒店区域建议与推荐理由
- [x] 3.4 支持对关键偏好约束做再次调整，例如避免夜间驾驶、节奏松弛度或兴趣倾向

## 4. 分享页生成链路

- [x] 4.1 定义从已选 `planDraft.option` 到最终 `trip` 数据的映射函数或转换流程
- [x] 4.2 复用现有 `guide.js` 与攻略页视觉系统渲染最终分享页
- [x] 4.3 在首页合集中接入新生成旅程的展示入口，保证新旧旅程可以共存

## 5. 偏好记忆 MVP

- [x] 5.1 从历史 `trip` 数据中提取显性偏好信号，例如天数、交通倾向、酒店风格和内容取向
- [x] 5.2 从规划过程中的用户调整记录中提取行为信号，并合并到 `preferenceProfile`
- [x] 5.3 在生成候选方案时读取 `preferenceProfile`，输出带解释的推荐理由

## 6. 边界控制与回归验证

- [x] 6.1 明确系统可默认决定与必须确认的字段校验逻辑，尤其是人数、预算与出行风险
- [x] 6.2 确保流程中不进入订票、支付、自动代下单或海外复杂行程等非目标能力
- [x] 6.3 验证首页、`/planner`、最终分享页三段链路可以共存，且现有静态旅程不回归损坏

## 7. 移动端体验优化与验证

- [x] 7.1 将 `/planner` 流程重构为移动端分步式：必填信息、偏好微调、候选方案、发布确认、发布完成
- [x] 7.2 增加移动端效率能力：快捷模板、草稿恢复、底部 sticky 主操作区
- [x] 7.3 提升决策透明度：选择后展示与次优方案差异提示，统一用户导向文案
- [x] 7.4 增加发布闭环：发布确认层 + 发布成功态（查看分享页 / 返回首页）
- [x] 7.5 用 Playwright 覆盖移动端关键操作路径并产出截图报告

## 8. MVP 补充优化（移动端与实时信息）

- [x] 8.1 修复“生成方案”反馈时机：点击后即时显示查询中状态，避免空白等待
- [x] 8.2 增加交通手动覆盖能力：支持在方案内直接切换交通，并在发布时按最终选择落地
- [x] 8.3 优化交通展示：合并重复字段，自驾突出高速路线、里程与高速费
- [x] 8.4 天气改为按出发日+天数逐日展示，增加天气小图标
- [x] 8.5 修复节假日文案映射，确保“出发日类型”与真实日期一致
- [x] 8.6 增强来源标识：高德实时数据和 AI 推荐展示明确标记
- [x] 8.7 收敛移动端视觉：优化日期控件样式与交通选中高亮强度
